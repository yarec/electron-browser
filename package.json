{
  "name": "electron-browser",
  "version": "1.2.1",
  "main": "index.js",
  "description": "browser ui for electron, written in react",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "electron",
    "browser",
    "webview",
    "react"
  ],
  "author": "Paul Frazee <pfrazee@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^8.3.0",
    "electron-prebuilt": "^0.28.2"
  },
  "dependencies": {

  },
  "scripts" : {
    "packager": "electron-packager ./ HelloWorld --platform=win32 --arch=x64 --out ./OutApp --version 1.4.7 --overwrite "
  }
}
